
<h4><%= "#{@publication.name} - #{display_date(@publication.date)}" %></h4>
<div class="row">
  <div class="col m4">
    <p><%= "Ads booked: #{@publication.ads.count}" %></p>
  </div>
  <div class="col m4">
    <p><%= "Gross: #{@gross}" %></p>
  </div>
</div>
